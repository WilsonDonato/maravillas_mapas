<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.maravillasdelmundowilsonholguin">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/plaza"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iglesia"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaravillasDelMundoWilsonHolguin">
        <activity
            android:name=".rutas"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".comidas"
            android:exported="true"
            android:label="@string/title_activity_comidas" />
        <activity
            android:name=".mapasmodernos"
            android:exported="true"
            android:label="@string/title_activity_mapasmodernos" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_activity_mapasmodernos" />
        <activity
            android:name=".mapasantiguos"
            android:exported="true"
            android:label="@string/title_activity_mapasantiguos" />
        <activity
            android:name=".maps_cusco"
            android:exported="true"
            android:label="@string/title_activity_maps_cusco" />
        <activity
            android:name=".splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>